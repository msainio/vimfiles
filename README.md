# vimfiles

This repository contains files for configuring the [Vim text editor](https://github.com/vim/vim).
The files can be installed by running the command below.

```bash
git clone --recurse-submodules https://github.com/msainio/vimfiles.git ~/.vim
```
